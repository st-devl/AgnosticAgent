# Quality Policy
# Controls: clean code, minimal complexity, no duplicates

version: "1.0.0"
status: active

thresholds:
  max_function_lines: 50
  max_file_lines: 500
  max_nesting_depth: 4
  min_duplicate_lines: 5

actions:
  on_violation:
    severity: warning
    block: false
    notify: true
